version: "3.9"
services:
  core-shop-service:
    build:
      context: .
      dockerfile: ./src/core-shop-service/Dockerfile
    ports:
      - "8080:8080"

  user-auth-service:
    build:
      context: .
      dockerfile: ./src/user-auth-service/Dockerfile
    ports:
      - "8081:8081"

    